"""Django app configuration for the API application."""

from django.apps import AppConfig


class AppConfig(AppConfig):
    """Application configuration for climate data models and views."""

    name = "api"
